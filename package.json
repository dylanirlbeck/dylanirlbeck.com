{
  "scripts": {
    "dev": "yarn parcel *.html *.css",
    "build": "yarn parcel build *.html *.css",
    "post": "node scripts/createPost.js"
  },
  "devDependencies": {
    "parcel": "^2.0.1",
    "prettier": "2.5.1"
  },
  "dependencies": {
    "showdown": "^1.9.1"
  }
}
